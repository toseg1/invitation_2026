# Access password for the invitation cards
VITE_ACCESS_PASSWORD=your_password

# API URL for RSVP backend (leave empty for localhost:5002 in development)
VITE_API_URL=

# Email Configuration (for sending RSVP confirmations)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SENDER_EMAIL=your-email@gmail.com
SENDER_PASSWORD=your-app-password

# Database Configuration (Production only - leave empty for local SQLite)
DATABASE_URL=
